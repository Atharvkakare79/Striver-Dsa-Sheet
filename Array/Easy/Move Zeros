class Solution {
    public void moveZeroes(int[] nums) {
        int initial=0;
        for (int i=0;i<nums.length;i++){
            if(nums[i]!=0){
                nums[initial]=nums[i];
                  initial++;
            }

        }
        for(int i=initial;i<nums.length;i++){
            nums[i]=0;
        }
        
    }
}
